# 🏛️ CyprusGuard — Инструкция по настройке

## Структура проекта

```
cypress-home-checkup/
├── index.html          ← Админ-панель (PWA)
├── client.html         ← Клиентский портал (PWA)
├── style.css           ← Общие стили
├── admin.css           ← Стили админки
├── admin.js            ← Логика админки
├── client.css          ← Стили клиента
├── client.js           ← Логика клиента
├── firebase-config.js  ← ⚠️ СЮДА вставить конфиг Firebase
├── sw.js               ← Service Worker (офлайн PWA)
├── manifest.json       ← PWA манифест (админ)
├── manifest-client.json← PWA манифест (клиент)
├── icons/              ← Иконки приложения
│   ├── icon-192.png
│   └── icon-512.png
└── bot/
    ├── index.js        ← Telegram бот
    ├── package.json
    └── README.md       ← Инструкция по деплою бота
```

---

## Шаг 1 — Создать Firebase проект

1. Перейдите на [console.firebase.google.com](https://console.firebase.google.com)
2. **Add project** → введите название (напр. `cyprusguard`)
3. Google Analytics → можно отключить → **Create project**

---

## Шаг 2 — Включить Realtime Database

1. В левом меню: **Build → Realtime Database**
2. **Create database** → выберите регион **Europe (belgium)** → **Start in test mode**
3. Скопируйте URL базы данных: `https://cyprusguard-default-rtdb.europe-west1.firebasedatabase.app`

**Правила безопасности** (вставьте в Rules):
```json
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "clients": {
      ".read": true,
      ".indexOn": ["accessToken", "tgChatId", "tg"]
    }
  }
}
```

---

## Шаг 3 — Включить Authentication

1. **Build → Authentication → Get started**
2. Вкладка **Sign-in method** → включите **Email/Password**
3. Вкладка **Users → Add user**:
   - Email: `admin@cyprusguard.com`
   - Password: придумайте надёжный пароль

---

## Шаг 4 — Включить Storage

1. **Build → Storage → Get started** → **Start in test mode**
2. Регион: `europe-west` → **Done**

---

## Шаг 5 — Вставить конфиг в firebase-config.js

1. **Project Settings (⚙️) → General → Your apps → Add app → Web (</> )**
2. Введите название (напр. `CyprusGuard Web`) → **Register app**
3. Скопируйте объект `firebaseConfig`
4. Откройте `firebase-config.js` и замените блок в начале файла:

```javascript
const firebaseConfig = {
  apiKey:            "AIzaSy...",
  authDomain:        "cyprusguard.firebaseapp.com",
  databaseURL:       "https://cyprusguard-default-rtdb.europe-west1.firebasedatabase.app",
  projectId:         "cyprusguard",
  storageBucket:     "cyprusguard.appspot.com",
  messagingSenderId: "123456789",
  appId:             "1:123...:web:abc..."
};
```

---

## Шаг 6 — Задеплоить фронтенд

### Вариант A: GitHub Pages (бесплатно, просто)

1. Добавьте все файлы в GitHub репозиторий
2. **Settings → Pages → Source: Deploy from branch → main → / (root)**
3. Через 2-3 минуты сайт будет на: `https://username.github.io/repo-name/`

### Вариант B: Netlify (рекомендуется — поддерживает PWA лучше)

1. Зайдите на [netlify.com](https://netlify.com)
2. **Add new site → Import from Git → GitHub**
3. Выберите репозиторий → **Deploy site**
4. Домен будет: `https://your-site.netlify.app`

### Вариант C: Firebase Hosting (в комплекте с Firebase)

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
# Public directory: . (точка)
# Single-page app: No
firebase deploy
```

---

## Шаг 7 — Первый запуск

1. Откройте `index.html` (или ваш деплой)
2. Войдите с email/паролем из Шага 3
3. Откройте консоль браузера (F12) и выполните:
   ```javascript
   seedDemoData()
   ```
   Это создаст демо-данные (клиенты, объекты, настройки)
4. Обновите страницу — данные появятся в интерфейсе

---

## Шаг 8 — Настройка Telegram бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. `/newbot` → введите имя → введите username (напр. `CyprusGuardBot`)
3. Скопируйте токен
4. В админке перейдите в раздел **Telegram Bot**
5. Вставьте токен → **Сохранить**
6. Задеплойте бота по инструкции в `bot/README.md`
7. Напишите своему боту `/start` — он сохранит ваш admin chat_id

---

## Шаг 9 — Добавить клиента и выдать ссылку

1. Перейдите в раздел **Клиенты → + Добавить**
2. Заполните: имя, Telegram username, страна
3. Нажмите **🔗 Портал** рядом с клиентом
4. Скопируйте ссылку вида:
   ```
   https://your-site.netlify.app/client.html?token=tok-abc123
   ```
5. Отправьте клиенту — он сразу попадёт в личный кабинет без регистрации

---

## Шаг 10 — Установка как PWA

**Для себя (администратор):**
- Chrome Desktop: иконка ⊕ в адресной строке
- Android Chrome: «Добавить на главный экран»
- iOS Safari: Поделиться (□↑) → «На экран Домой»

**Для клиента** (покажите/пришлите инструкцию):
- Он открывает ссылку клиентского портала
- Вкладка **ℹ️ Контакты** → кнопка «Установить приложение»
- Или через браузер как описано выше

---

## Бюджет (всё бесплатно)

| Сервис | Бесплатный лимит | Стоимость при превышении |
|--------|-----------------|--------------------------|
| Firebase Realtime DB | 1 ГБ хранилища, 10 ГБ/мес трафик | $5/ГБ |
| Firebase Storage | 5 ГБ, 1 ГБ/день загрузок | $0.026/ГБ |
| Firebase Auth | 10 000 аутентификаций/мес | Бесплатно |
| Netlify Hosting | 100 ГБ трафика/мес | $19/мес |
| Railway (бот) | 500 часов/мес | $5/мес |

Для 10-15 клиентов всё бесплатно ~бессрочно.

---

## Часто задаваемые вопросы

**Q: Клиент не видит свои данные**
A: Убедитесь, что в разделе «Клиенты» указан правильный clientId у объектов

**Q: Telegram бот не отвечает**
A: Проверьте логи на Railway/Render и правильность переменных окружения

**Q: Фото не загружаются**
A: Убедитесь, что Storage включён и правила разрешают запись

**Q: PWA не устанавливается на iOS**
A: iOS требует HTTPS. Убедитесь, что сайт деплоен (не открывается как file://)

---

## 🆕 Что нового в этой версии

### Финансы
- **Реальное время** — счета и KPI обновляются мгновенно (раньше только при заходе на вкладку).
- **Статусы счетов** — кнопка «Оплачен / Сбросить» прямо в списке.
- **Авто-просрочка** — неоплаченные счета прошлых месяцев автоматически помечаются «Просрочен».
- **Генерация счетов за месяц** — кнопка «＋ Счета за месяц» создаёт счета по всем объектам (без дублей).
- **PDF-счёт** — кнопка «📄 PDF» открывает печатную форму счёта (печать / сохранение в PDF).
- **График доходности** строится из реально оплаченных счетов; пунктиром показывается прогноз, пока истории нет.

### Объекты и визиты
- **Карточка объекта** — полноценная модалка: статистика, контакты клиента, история визитов и отчётов, кнопки «Изменить» / «Удалить» / «＋ Визит».
- **Карточка визита** — модалка с задачами, заметками, статусом, переходом к отчёту и удалением.
- **Кликабельный календарь** — клик по дню открывает визит или форму создания на эту дату.
- **Календарь** открывается на текущем месяце (раньше был жёстко зашит май 2026).
- Визит закрывается только после сохранения отчёта (убрана двойная логика).

### Фото и хранилище
- Фотографии в отчётах **сжимаются** перед сохранением (общий модуль `compressImage`) — база не раздувается на бесплатном плане Firebase.
- Удаление фото из формы исправлено (раньше при удалении среднего фото удалялось не то).

### Клиентский портал
- **4 языка интерфейса** — RU / EN / DE / FR, переключатель в шапке. Язык берётся из карточки клиента и сохраняется при смене.
- Даты, статусы, состояния и описания тарифов локализованы.

### Telegram-бот
- **Напоминание за 24 часа** до визита (ежечасная проверка, учитывает тумблер «Напоминание за 24ч»).
- Срочные заявки помечаются 🚨 в уведомлении администратору.

### Прочее
- **Живая погода** в Лимассоле на дашборде (Open-Meteo, без API-ключа).
- Service Worker обновлён до стратегии **Network-First** для кода — обновления подхватываются без переустановки PWA (версия кэша `v3`).

---

## 🆕 Итерация 2

### PDF отчёта о визите
- В карточке отчёта — кнопка «📄 PDF»: печатная форма с состоянием объекта, выполненными работами, фотофиксацией, комментарием агента и счётом ЖКХ. Открывается в новом окне с авто-печатью (Сохранить как PDF).

### Повторяющиеся визиты по тарифу
- В карточке объекта — кнопка «🔁 Визиты по тарифу»: генерирует график на месяц вперёд.
  - Basic — раз в 2 недели (≈2 визита/мес)
  - Standard / Premium — еженедельно (≈4 визита/мес)
- Набор задач подставляется по тарифу. Уже существующие визиты на те же даты не дублируются. Клиент получает уведомление в Telegram.

### Карточка клиента
- Клик по клиенту открывает детальную карточку: доход/мес (считается из тарифов объектов), число объектов и отчётов, контакты, список объектов и последних отчётов с переходами, статус подключения к Telegram.
- Кнопки «✎ Изменить» (имя, страна, телефон, Telegram, язык) и «🗑 Удалить» (только если у клиента нет объектов). Кнопка «🔗 Портал» — прежняя ссылка на клиентский кабинет.

### Мультиязычный Telegram-бот
- Бот отвечает клиенту на его языке (RU / EN / DE / FR) — берётся из карточки клиента.
- Клавиатура и все сообщения локализованы; команды распознаются по эмодзи, поэтому работают на любом языке.
- Ключевое слово заявки понимается на всех языках: «Заявка:», «Request:», «Anfrage:», «Demande:».

---

## 🛠 Итерация 3 — исправления

- **Календарь**: клик по дню с несколькими визитами теперь открывает список выбора (а не первый визит).
- **Заявки**: исправлен цикл статусов. Новая → В работе → Выполнена; у выполненной кнопка теперь «↺ Вернуть в работу» (раньше ошибочно показывалась «Принять в работу»).
- **Финансы (мобильные)**: строка счёта стала адаптивной — на узких экранах статус и кнопки «Оплачен/Сбросить» и «PDF» переносятся на отдельный ряд и больше не вылезают за край.
- **Telegram-бот**: при старте сбрасывается webhook (частая причина «бот молчит»), добавлены понятные диагностические сообщения в лог (конфликт 409 двух копий, неверный токен 401, сетевые сбои). Если бот не отвечает — смотрите логи хостинга, теперь причина видна сразу.

### Если бот всё ещё молчит — проверьте по порядку:
1. Бот задеплоен и **запущен один раз** (не две копии с одним токеном → ошибка 409 в логах).
2. В переменных окружения заданы `BOT_TOKEN`, `ADMIN_CHAT_ID`, `FIREBASE_DB_URL`, `FIREBASE_SERVICE_ACCOUNT_JSON`.
3. На бесплатном хостинге (Render Free) сервис не «уснул».
4. В логах после старта есть строки: `✅ Firebase connected`, `🧹 Webhook cleared`, `🤖 Bot started`.

---

## 🛠 Итерация 4 — исправления по логам

### Бот падал при обрыве сети (ECONNRESET)
В логах был `[polling_error] EFATAL: read ECONNRESET` — соединение с Telegram оборвалось и polling больше не восстанавливался (бот «жив», но не отвечает). Теперь бот **сам перезапускает polling** при сетевых сбоях (`ECONNRESET`, `ETIMEDOUT`, `EFATAL` и т.п.), а на конфликт 409 — наоборот, не перезапускается, чтобы не воевать со второй копией. Добавлены `keepAlive` и перехват `uncaughtException`.
> На бесплатных хостингах сеть «моргает» регулярно — теперь это не убивает бота. Если используете Render Free, учтите, что сервис ещё и засыпает без активности: для бота лучше платный план или Railway.

### Service Worker падал на chrome-extension запросах
Ошибка из консоли `Failed to execute 'put' on 'Cache': Request scheme 'chrome-extension' is unsupported` — SW пытался кэшировать запросы расширений браузера. Теперь SW обрабатывает только `http(s)`-запросы, а `cache.put` защищён. (кэш `v6`)

### Предупреждения Firmware про индексы
`FIREBASE WARNING: Using an unspecified index … add ".indexOn": "createdAt"` — это предупреждение о производительности (без индекса БД скачивает все записи и фильтрует на клиенте). Добавлен файл **`database.rules.json`** с нужными индексами.

**Как применить правила:**
1. Firebase Console → ваш проект → **Realtime Database** → вкладка **Rules**.
2. Вставьте содержимое `database.rules.json` (можно без строки `_comment`).
3. Нажмите **Publish**.

⚠️ Файл сохраняет ОТКРЫТЫЙ доступ (`.read/.write: true`), потому что клиентский портал читает БД без входа (по токену из ссылки). Не закрывайте доступ на `auth != null` — иначе портал перестанет работать без отдельной доработки авторизации.

### Мелочь
Добавлен современный `<meta name="mobile-web-app-capable">` рядом с устаревшим apple-тегом (убирает warning в консоли).

---

## 🆕 Итерация 5 — авто-уведомления и управление из бота

### Авто-уведомления теперь работают надёжно
Раньше уведомление о новой заявке могло не прийти из-за привязки к времени старта бота. Переписано: при старте бот один раз помечает все существующие заявки/отчёты как «уже уведомлённые» (чтобы не спамить бэклогом), а дальше реагирует на КАЖДУЮ новую запись. Теперь:
- **Новая заявка с сайта** → админу сразу приходит уведомление.
- **Новый отчёт** → админу приходит уведомление с кнопкой «✈ Отправить клиенту».

### Управление статусами прямо из чата (inline-кнопки)
- Под уведомлением о заявке — кнопки **«🔧 В работу»** и **«✅ Выполнено»**. Нажатие меняет статус заявки в базе (сайт видит сразу, realtime).
- Под уведомлением об отчёте — кнопка **«✈ Отправить клиенту»**: бот отправляет отчёт клиенту в Telegram на его языке и помечает отчёт отправленным.

### Поле «Telegram Chat ID» в карточке клиента
Теперь клиента можно подключить к боту вручную, даже если у него нет @username:
1. Клиент пишет боту `/start` → бот отвечает «ваш chat ID: …».
2. Впишите этот ID в карточке клиента (Изменить → Telegram Chat ID).
3. Клиент подключён — будет получать напоминания и отчёты.

> Если у клиента есть @username и он указан в карточке — подключение по-прежнему происходит автоматически при `/start`.

---

## 🆕 Итерация 6 — клиент узнаёт о смене статуса заявки

Теперь когда статус заявки меняется (кнопкой в боте «🔧 В работу»/«✅ Выполнено» ИЛИ на сайте) — клиенту автоматически приходит уведомление в Telegram на его языке:
- «🔧 Ваша заявка взята в работу…»
- «✅ Ваша заявка выполнена…»

Работает только если клиент подключён к боту (🟢). Уведомление отправляется один раз на каждое изменение статуса (защита от дублей). Реализовано через единый `child_changed` listener, поэтому срабатывает одинаково и из бота, и с сайта.

---

## 🆕 Итерация 7 — аналитика, экспорт, автосчета, безопасность

### Дашборд-аналитика
На Дашборде добавлен блок «Аналитика»: доход по тарифам (Basic/Standard/Premium), состояние объектов (норма/внимание/проблема), выполнение визитов за текущий месяц.

### Поиск и экспорт
- Поиск по клиентам (имя, страна, телефон, Telegram) и по отчётам (по адресу). Поиск по объектам уже был.
- Экспорт в CSV: кнопка «⬇ CSV» на вкладках Клиенты и Финансы (открывается в Excel, разделитель `;`, с BOM для кириллицы).

### Авто-счета и напоминания об оплате (в боте)
Бот раз в час проверяет:
- **1-го числа месяца** — автоматически создаёт счета по всем объектам (без дублей) и присылает админу сводку.
- **Просроченные счета** — pending-счёт из прошлого месяца помечается overdue, и клиенту один раз уходит напоминание об оплате в Telegram на его языке.
> Формат ID счёта одинаков у сайта и бота, поэтому ручная и авто-генерация не конфликтуют.

### Безопасность портала (требует действия в Firebase!)
Клиентский портал теперь входит **анонимно** (`Auth.ensureAnonAuth`), что позволяет закрыть базу правилом `auth != null`. Файл `database.rules.json` обновлён до защищённой версии.

**⚠️ Чтобы это заработало, выполните ДВА шага в Firebase Console (иначе портал перестанет читать данные):**
1. **Authentication → Sign-in method → Anonymous → Enable.**
2. **Realtime Database → Rules** → вставьте содержимое `database.rules.json` (без строки `_comment_ru`) → **Publish.**

После этого база закрыта от анонимного доступа из интернета: читать/писать могут только админ (email), портал (анонимно) и бот (service account).

---

## 🛠 Итерация 8 — исправления

1. **Бот: переключение статуса заявки.** После «🔧 В работу» теперь появляется кнопка «✅ Выполнено» (раньше кнопка пропадала и завершить из бота было нельзя). После «Выполнено» кнопки убираются.
2. **Отправка отчёта клиенту («✈ Отправить в Telegram»)** переделана на надёжный путь через бота: сайт ставит флаг `sendToClient`, бот доставляет отчёт клиенту (текст на его языке + фото по публичным URL). Прямая отправка из браузера оставлена как fallback. Если у клиента нет Telegram или токена — теперь показывается понятная причина, а не тишина.
3. **Фото во втором отчёте.** Исправлен сброс формы: при открытии нового отчёта плейсхолдер загрузки фото возвращается, и фото снова прикрепляются (раньше после первого отчёта поле фото оставалось «скрытым»).
4. **Доход клиента €0** — это не баг: доход считается из тарифов привязанных объектов. У клиентов без объектов он и есть €0. Появится после привязки объекта.

> Примечание про фото в Telegram: на бесплатном плане Firebase фото хранятся как base64 в базе (не публичный URL), поэтому бот отправляет клиенту текст отчёта, а фото — только если они доступны по http-ссылке. Полная отправка фото потребует Firebase Storage (платный план) или другого файлового хранилища.

---

## 🆕 Итерация 9 — турецкий язык + переключатель языка

### Турецкий язык (Türkçe)
Добавлен 5-й язык — турецкий — во всех трёх частях: клиентский портал, выбор языка в карточке клиента (админка) и Telegram-бот (сообщения, клавиатура, ключевое слово заявки «Talep:»). Актуально для Северного Кипра.

### Переключатель языка в портале
- Убраны флаги-эмодзи (на Windows они показывались как «RU RU», «GB EN» — система не рендерит флаги).
- Теперь полные названия: Русский / English / Deutsch / Français / Türkçe.
- Улучшена видимость: золотая рамка, белый текст, стрелка-индикатор, тёмный текст в выпадающем списке.

### Напоминание про бота
Если в боте под заявкой только одна кнопка «В работу» (без «Выполнено») — значит на Railway ещё старый код. Залейте свежий `bot/index.js` (BUILD v14) — кнопка статуса будет переключаться корректно.

---

## 🆕 Итерация 10 — уведомления о статусе, /help, дашборд-оплаты

1. **Уведомление админу при смене статуса с сайта.** Когда статус заявки меняется в приложении — бот присылает админу короткое «ℹ️ Статус заявки изменён». Если статус меняли кнопкой в самом боте — повторного уведомления нет (админ и так это видел). Клиенту уведомление приходит в обоих случаях.
2. **Бот: команда `/help`** — показывает список команд (для админа — админские, для клиента — подсказку по заявке).
3. **Карточка клиента** — если у клиента нет объектов, вместо «€0 в месяц» показывается «нет объектов».
4. **Дашборд: блок «Оплаты»** — если есть просроченные/ожидающие счета, на дашборде появляется карточка с суммами и переходом в Финансы (просроченные подсвечены красным).

---

## 🆕 Итерация 11 — графика, мобильная кнопка, кликабельные KPI

### Графика
- **Освежение визуала**: мягкие тени у карточек, подъём карточек при наведении, плавная анимация появления страниц и модалок, отклик кнопок при нажатии.
- **SVG-иконки** в боковом меню вместо символов/эмодзи — единый аккуратный стиль.
- **Светлая тема**: кнопка-переключатель 🌙/☀️ в шапке. Выбор запоминается (localStorage).
- **Брендинг**: в Настройках — выбор фирменного цвета (применяется ко всему интерфейсу мгновенно, сохраняется). Название/город/телефон агентства уже были (используются в PDF).

### Исправления юзабилити
- **Мобильная кнопка «Добавить»**: на телефоне кнопка ＋ в шапке скрывалась, а плавающей кнопки (FAB) не было в разметке — поэтому добавить объект/визит/клиента с телефона было нельзя. Добавлена круглая кнопка ＋ внизу справа (FAB).
- **Кликабельные KPI на дашборде**: карточки Объектов/Доход/Визитов/Заявок теперь ведут на соответствующие разделы.

### Бот
- **`/testbilling`** — запустить проверку биллинга вручную (создаёт счета за текущий месяц + обрабатывает просрочки), не дожидаясь 1-го числа.
- Напоминание: автосчета и так создаются автоматически 1-го числа каждого месяца.

---

## 🛠 Итерация 12 — доработка светлой темы

Светлая тема переработана: раньше фон body и часть градиентов были «зашиты» тёмными — поэтому в светлой теме карточки становились белыми на тёмном фоне (выглядело сломанно). Теперь:
- Фон страницы использует переменную темы (светлый фон в светлой теме).
- Hero и карточка «Объектов» получили корректные светлые градиенты (синий на белом).
- Палитра светлой темы сделана тёплой и мягкой (не режущий чисто-белый), золотой акцент затемнён для читаемости на светлом.
- Сайдбар, топбар, кнопки, модалки — все на переменных, переключаются целиком.

> Клиентский портал пока остаётся в тёмной теме (у клиентов нет переключателя). При желании можно добавить и туда.

### Как проверить выставление счетов
- **Бот:** команда `/testbilling` — создаёт счета за текущий месяц и обрабатывает просрочки сразу.
- **Сайт:** Финансы → «＋ Счета за месяц».
- Результат смотреть в разделе Финансы (статусы, кнопки оплаты, PDF). Автоматически счета создаются 1-го числа каждого месяца.

---

## 🆕 Итерация 13 — светлая тема портала, логотип, мобильная полировка

### 1. Светлая тема в клиентском портале
В портал добавлена кнопка 🌙/☀️ (рядом с выбором языка). Тема запоминается. Градиенты hero и теги исправлены для светлого режима.

### 2. Логотип агентства
В Настройках — загрузка логотипа (📷 Загрузить логотип). Картинка сжимается до 256px и сохраняется. Логотип отображается: в шапке админки (вместо 🏛️), в PDF-шапках счёта и отчёта. Кнопка «Убрать» возвращает иконку по умолчанию. Не забудьте нажать «Сохранить» после загрузки.

### 3. Полировка мобильной версии
- Модальные окна на телефоне открываются снизу (как нативные шторки), со скроллом тела и «липкими» кнопками внизу.
- Поля ввода — 16px (отключает авто-зум iOS), тач-таргеты увеличены (кнопки/пункты меню ≥44px).
- Аналитика на телефоне в один столбец; на очень узких экранах (≤380px) KPI тоже в столбец.
- Те же улучшения применены к клиентскому порталу (вкладки, кнопки, модалки).

---

## 🆕 Итерация 13 — светлая тема портала, логотип, мобильная полировка

- **Светлая тема в клиентском портале**: переключатель 🌙/☀️ в шапке портала, корректные светлые градиенты hero-карточки, единый стиль с админкой. Выбор сохраняется.
- **Логотип агентства**: в Настройках загрузка картинки-логотипа (сжимается автоматически). Логотип показывается в шапке админки и в PDF счетов/отчётов вместо иконки 🏛️.
- **Мобильная полировка**: учёт «безопасных зон» на телефонах с вырезом (notch), запрет горизонтальной прокрутки, тосты над плавающей кнопкой, адаптация модалок в горизонтальной ориентации, уменьшение текста бренда в шапке на узких экранах.

### Исправления
- **Финансы: кнопка «＋»** теперь создаёт счета за месяц (раньше открывала форму визита).

---

## 🆕 Итерация 14 — создание данных прямо в боте

В админскую клавиатуру добавлена кнопка **«➕ Создать»** — теперь админ может создавать всё прямо в Telegram через пошаговые диалоги:

- **👤 Клиент**: имя → страна → телефон → @username → язык (кнопки).
- **🏠 Объект**: адрес → клиент (кнопки) → тип (кнопки) → тариф (кнопки) → заметка.
- **📅 Визит**: выбор объекта → тип → дата (ГГГГ-ММ-ДД) → заметка.
- **📋 Отчёт**: выбор объекта → состояние (✅/⚠️/❌) → комментарий. Состояние применяется к объекту.

Любой шаг можно прервать кнопкой «❌ Отмена». Созданные записи сразу появляются в приложении (realtime).

> Примечание: загрузка фото в отчёт через бота не поддерживается (фото удобнее добавлять в приложении). Отчёт из бота создаётся без фото. Также автозаявки-уведомления для созданных ботом отчётов подавлены (чтобы админ не получал уведомление о том, что создал сам).

### Также исправлено
- Отказоустойчивый warm-up: сбой при старте больше не «вешает» уведомления навсегда (+ страховка через 30 сек).

---

## 🆕 Итерация 15 — полная синхронизация уведомлений приложение → бот

Теперь бот уведомляет админа о ВС�ех значимых действиях, выполненных в приложении:
- 🏠 **Новый объект** добавлен
- 📅 **Новый визит** запланирован
- 👤 **Новый клиент** добавлен
- 📬 Новая заявка (было)
- 📋 Новый отчёт (было)
- 🔄 Смена статуса заявки (было)

Реализация: бот уведомляет только о записях, созданных ПОСЛЕ его старта (по таймстампу в ID), чтобы при перезапуске не приходила лавина уведомлений о старых данных. Записи, созданные самим ботом (через «➕ Создать»), помечаются `_fromBot` и не дублируют уведомление админу.

> Двусторонняя связь: бот → приложение (создание/статусы) и приложение → бот (уведомления обо всём) теперь работают в обе стороны.

---

## 🆕 Итерация 16 — напоминания, аналитика, онлайн-оплата, (фото в TG)

### ☀️ Утренние напоминания агенту (бот)
Бот каждое утро (08–10ч) присылает админу список визитов на сегодня. Команда `/today` или кнопка «📅 Сегодня» — показать агенду в любой момент.

### 📊 Финансовая аналитика (приложение)
На странице Финансы добавлен блок «Должники» — клиенты с неоплаченными/просроченными счетами, сгруппированы и отсортированы по сумме (просрочка подсвечена). График доходности по 6 месяцам уже был.

### 💳 Онлайн-оплата (через платёжную ссылку)
Самый простой рабочий путь без бэкенда. В Настройках → поле «Ссылка на оплату». Вставьте туда вашу платёжную ссылку:
- **Revolut**: revolut.me/ваш-ник
- **Stripe Payment Link**: создаётся в дашборде Stripe за 1 минуту (Products → Payment links)
- **Wise / банковская ссылка** — любая постоянная ссылка на оплату.

После сохранения у клиента в портале появляется кнопка «💳 Оплатить обслуживание» (на его языке), ведущая на эту ссылку.

> Полноценный встроенный эквайринг (оплата прямо внутри портала с автоотметкой счёта) потребовал бы бэкенд-сервера и webhook'ов от платёжной системы — это отдельный проект. Платёжная ссылка решает 90% задачи сразу и бесплатно.

### 📷 Фото-отчёты в Telegram — ТРЕБУЕТСЯ ДЕЙСТВИЕ
Сейчас фото в отчётах хранятся прямо в базе (base64), и Telegram не может их скачать по ссылке — поэтому бот шлёт клиенту текст отчёта, а фото только если это http-URL.
Чтобы фото уходили в Telegram, нужно включить **Firebase Storage**:
1. Это требует перехода на тарифный план **Blaze** (оплата по факту использования; для малых объёмов — копейки, но карта привязывается).
2. После включения Storage код можно доработать: фото будут грузиться в Storage, получать публичный URL, и бот сможет их отправлять.
Без перехода на Blaze фото в TG технически невозможны. Решение за вами — если включите Blaze, скажите, доработаю загрузку.

---

## 🆕 Итерация 17 — города, карта, быстрая привязка

### 📍 Фильтр по городам Кипра
В форме объекта добавлено поле «Город» (Лимассол / Пафос / Ларнака / Никосия / Фамагуста / Кирения). В разделе Объекты — чипы-фильтры по городам, город показывается на карточке и участвует в поиске.

### 🗺 Карта объектов
В разделе Объекты — переключатель «Список / Карта». Карта (OpenStreetMap, бесплатно) показывает объекты маркерами по городам, цвет маркера = статус (зелёный/жёлтый/красный), клик по маркеру — инфо об объекте. Маркеры ставятся по городу объекта (приблизительно), поэтому у объекта должен быть указан город.

### 🔗 Быстрая привязка пользователя бота
Когда новый человек пишет боту /start и его нет в базе, админу приходит сообщение «Новый пользователь @x, Chat ID: …» с кнопками: привязать к существующему клиенту или создать нового (с автопривязкой). Больше не нужно вручную копировать Chat ID.

### Отложено на отдельные заходы (по объёму/риску)
- Двуязычная админка RU/EN (≈136 строк в разметке + динамика в JS — делается отдельно и тщательно).
- Роли SuperAdmin/Inspector (нужны отдельные аккаунты инспекторов — обсудить флоу).
- Офлайн-режим Background Sync (переработка слоя данных + IndexedDB — высокий риск регрессий, делать изолированно).

---

## 🆕 Итерация 18 — фикс карты на мобиле + офлайн-режим

### Карта на телефоне
Исправлено: карта не отображалась на мобильных (Leaflet инициализировался до получения размеров контейнера). Добавлен многократный пересчёт размера и мобильные CSS-правила. Теперь карта работает и на ПК, и на телефоне.

### 📡 Офлайн-режим
Задействована встроенная офлайн-очередь Firebase Realtime Database: изменения, сделанные без связи (создание визита, отчёта и т.д.), сохраняются локально и автоматически отправляются при восстановлении интернета. Ключевые узлы держатся синхронизированными для офлайн-чтения. Внизу экрана показывается баннер «📡 Нет связи — изменения сохранятся и отправятся автоматически», когда соединение пропадает (и в админке, и в портале).

> Это надёжнее самописной IndexedDB-очереди — синхронизацию берёт на себя SDK Firebase.

### Ещё в работе (нужен твой ответ)
- **Роли SuperAdmin/Inspector**: выберите путь — (А) отдельные Firebase-аккаунты для инспекторов с доступом в веб-админку, или (Б) инспекторы работают только через Telegram-бот (в вебе только вы). От этого зависит реализация.
- **Двуязычная админка RU/EN** — отдельным заходом.

---

## 🛠 Итерация 19 — фикс карты (наезд на меню)

Карта на мобиле перекрывала боковое меню (Leaflet использует высокий внутренний z-index). Исправлено: внутренние слои Leaflet прижаты ниже сайдбара, контейнер карты изолирован, и при открытом боковом меню карта скрывается. Теперь карта остаётся в своём разделе.

---

## 🆕 Итерация 20 — заметки по доступу + рейтинг от клиента

### 🔑 Заметки по доступу
В форме объекта новое поле «Доступ» (коды от ворот, где ключи, код сигнализации, контакт консьержа). В деталях объекта показывается в защищённом блоке с кнопкой «скрыть/показать» — чтобы случайно не показать чувствительные данные на экране.

### ⭐ Рейтинг отчёта от клиента
В клиентском портале в окне отчёта появились звёзды оценки (1–5). Клиент ставит оценку в один тап, оценка сохраняется и видна админу в карточке отчёта. Доступно на всех 5 языках.

### Уже было ранее (проверено)
- **Повторяющиеся визиты по расписанию** — кнопка в карточке объекта генерирует график визитов на месяц вперёд по интервалу тарифа (Basic — раз в 2 недели, Standard/Premium — еженедельно).
- **История объекта** — в деталях объекта список визитов и отчётов по нему (по сути таймлайн).

### Осталось из выбранного
- Фото «До/После» в отчёте — нужно уточнение, как реализовать (две группы фото или пометка у каждого).

---

## 🎯 Итерация 21 — маркетинговая страница и лиды

### Маркетинговая страница (landing.html)
Новый файл — посадочная страница агентства на 5 языках (RU/EN/DE/FR/TR с переключателем). Содержит:
- Hero-блок «Присматриваем за вашим домом, пока вы в отъезде»
- Раздел «Как это работает» (4 шага)
- Тарифы (Basic / Standard / Premium с автоматически переводящимися фичами)
- «Почему мы» (6 преимуществ)
- Форма заявки (имя, телефон, email, город, тариф, комментарий)

### Размещение
Файл `landing.html` лежит отдельно. Варианты публикации:
- **Простой:** оставить как есть, ссылка `сайт.netlify.app/landing.html`.
- **Главной:** в Netlify настроить redirect или переименовать (обсудим позже).

### Лиды в админке
Новый раздел «Лиды» в админ-панели (значок ⏱). Каждая заявка с лендинга → попадает сюда.
- Видны все поля (имя, телефон, email, город, тариф, комментарий, язык).
- Кнопка **«→ В клиенты»** — создаёт клиента из заявки одним кликом, сохраняя комментарий в заметки.
- Кнопка **«Отклонить»** — для непрофильных.
- Badge с числом новых лидов в меню.

### Уведомление админу в Telegram
Каждый новый лид → бот пишет тебе «🎯 Новая заявка с сайта» со всеми контактами. Не нужно держать админку открытой.

### Правила Firebase
Добавлен индекс для `leads`. **Опубликуйте обновлённый database.rules.json** в Firebase Console.

---

## 🤖 Итерация 22 — AI-помощник для отчётов

В форме отчёта появилась кнопка **«✨ AI-описание по фото»**. После прикрепления фото нажатие на кнопку анализирует их через Claude (модель Haiku) и добавляет описание состояния объекта в комментарий — инспектор лишь правит детали. Поддержка до 4 фото за раз.

### Как это работает безопасно
Браузер шлёт фото → HTTP-эндпоинт бота на Railway `/api/analyze-photo` → Claude API → текст обратно. **Ключ Anthropic хранится в переменной Railway**, в браузере его нет. Никто, кто откроет сайт, ключ не увидит.

### Что вам нужно сделать (один раз)
1. **Railway → Settings → Networking → Generate Domain** (если ещё не сгенерирован) — получите публичный адрес вашего бота, что-то вроде `https://your-bot.up.railway.app`.
2. **Админка → Настройки → AI-сервер** — вставьте этот адрес и нажмите Сохранить.
3. **Railway → Variables → ANTHROPIC_API_KEY** — должна быть переменная с вашим ключом Anthropic (если ещё не добавили).

После этого кнопка «✨ AI-описание» работает.

### Стоимость
Модель Haiku — порядка $0.0008 за одно фото-описание. То есть 1000 фото-описаний обойдутся примерно в $0.80. Лимиты можно задать в кабинете Anthropic.

### Эндпоинт /health
Проверка работоспособности: откройте в браузере `https://your-bot.up.railway.app/health` — должен вернуть `{"ok":true,"version":"v23-..."}`.
